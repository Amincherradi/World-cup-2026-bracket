# API-Football (api-sports.io) key — get a free one at https://dashboard.api-football.com
# Copy this file to `.env` and fill in your key. Without it the app falls back
# to the hand-entered static snapshot in src/data.js.
VITE_API_FOOTBALL_KEY=
